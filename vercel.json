{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "cd frontend && npx prisma generate --schema=prisma/schema.prisma && next build",
  "outputDirectory": "frontend/.next"
}
