import { ContactPage } from './ContactPage';

export const metadata = {
  title: 'Contacto — UrbanThread AI',
  description: 'Contáctanos para descubrir cómo UrbanThread AI puede transformar tu operación de retail.',
};

export default function ContactoPage() {
  return <ContactPage />;
}
