import { TestimoniosPage } from './TestimoniosPage';

export const metadata = {
  title: 'Testimonios — UrbanThread AI',
  description: 'Conoce las experiencias de empresas que transformaron su operación con UrbanThread AI.',
};

export default function TestimoniosRoute() {
  return <TestimoniosPage />;
}
