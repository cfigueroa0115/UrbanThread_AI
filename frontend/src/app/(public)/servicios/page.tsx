import { ServicesDetailPage } from './ServicesDetailPage';

export const metadata = {
  title: 'Servicios — UrbanThread AI',
  description: 'Descubre todos los servicios y capacidades de la plataforma Smart Commerce UrbanThread AI.',
};

export default function ServiciosPage() {
  return <ServicesDetailPage />;
}
